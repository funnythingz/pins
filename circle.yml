machine:
  ruby:
    version: 2.1.5
  node:
    version: 0.10.33
  timezone:
    Asia/Tokyo

dependencies:
  post:
    - bundle exec rake assets:precompile

test:
  override:
    - bundle exec rake bower:install
    - bundle exec rake spec
